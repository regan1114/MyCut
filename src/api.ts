import type { Media, Project } from '../shared/model';
declare global {interface Window {mycut?:{platform:'darwin'|'win32'|'linux';setEditorOpen:(active:boolean)=>Promise<void>;onCloseEditor:(callback:()=>void)=>()=>void;openProject:()=>Promise<Project|null>;packProject:(project:Project)=>Promise<boolean>;importFiles:()=>Promise<{media:Media[];errors:string[];imported:string[]}>;saveExport:(id:string)=>Promise<boolean>;relink:(id:string)=>Promise<Media[]>;reveal:()=>Promise<void>}}}
export async function api<T>(url:string,options:RequestInit={}):Promise<T>{const response=await fetch(url,{...options,headers:{'X-MyCut':'1',...(options.body&&typeof options.body==='string'?{'Content-Type':'application/json'}:{}),...options.headers}});if(!response.ok){const data=await response.json().catch(()=>({error:response.statusText}));throw new Error(data.error||'請求失敗');}return response.json();}
export function download(name:string,text:string,type='application/json'){const url=URL.createObjectURL(new Blob([text],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),3000);}
